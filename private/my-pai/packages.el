;;; packages.el --- my-pai layer packages file for Spacemacs. -*- lexical-binding: t; -*-
;;
;; pai (https://github.com/dejanmilivojevic/pai) and its extensions
;; (https://github.com/dejanmilivojevic/pai-extensions) are loaded from local
;; git clones inside this layer (see `config.el'), not from a package archive,
;; so this layer declares no ELPA/MELPA packages.

(defconst my-pai-packages '()
  "The list of Lisp packages required by the my-pai layer.")

;;; packages.el ends here
